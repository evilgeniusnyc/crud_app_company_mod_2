<h1> Create a New Assignment <h1> 

<hr> 

<%= form_for(@ssignment) do |f| %>
  <%= f.label :project_manager %>
  <%= f.collection_select :project_id, Project.all, :id, :name %>

  <%= f.label :project_manager %>
  <%= f.collection_select :project_manager_id, ProjectManager.all, :id, :name %><br>

  <br>

  <%= f.submit "Create New Assignment" %>  
<% end %> 
